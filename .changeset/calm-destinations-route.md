---
"@traice/collector": minor
---

Replace collector profiles and mirrors with workspace destinations and per-agent routes, add a readable routing map and multi-workspace device authorization support, default new logins to production, and provide one interactive setup command that detects supported agents.
