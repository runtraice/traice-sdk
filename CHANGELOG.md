# Changelog

## Unreleased

- Start public `@traice/*` monorepo.
- Move product SDK to `@traice/sdk`.
- Add `@traice/protocol`.
- Add `@traice/collector` foundation with Claude Code and Codex adapter targets.
- Port active exact-cache guardrails from the SaaS monorepo into `@traice/sdk`.
